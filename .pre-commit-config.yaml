repos:
  - repo: local
    hooks:
      - id: pixi-lock
        name: Update pixi.lock
        entry: pixi lock
        language: system
        files: ^pyproject\.toml$
        pass_filenames: false
